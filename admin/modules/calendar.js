document.addEventListener('DOMContentLoaded',()=>{const el=document.getElementById('dispatchCalendar');if(!el)return;if(window.FullCalendar){const c=new FullCalendar.Calendar(el,{initialView:'dayGridMonth',height:520,locale:'zh-tw',events:[{title:'桃機接送｜王小姐',start:new Date().toISOString().slice(0,10)},{title:'S2O 包車｜林先生',start:new Date(Date.now()+86400000).toISOString().slice(0,10)}]});c.render();}else{el.innerHTML='<p class="module-note">FullCalendar CDN 未載入。</p>';}});
